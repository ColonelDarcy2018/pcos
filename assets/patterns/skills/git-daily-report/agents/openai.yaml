interface:
  display_name: "Git Daily Report"
  short_description: "Strict git diff based daily report with auto sync"
  default_prompt: "Strictly use git diff HEAD to generate today's report (今日完成/明日任务/工时), write back to capture/journals, then commit all docs and push."
