# 小鹏公众号任务实现 Phase 1

- taskline_id: `rpa-mobile/outer/xpeng-gongzhonghao-phase1`
- project_id: `rpa-mobile`
- repo_root: `/Users/zhuxiaowei/apps/rpa-mobile`
- ccos_node: `outer`
- status: `in_progress`
- updated_at: `2026-04-09 08:55 +0800`
- updated_by: `codex(agent-codex-main)`

## 背景

项目内原始任务文档：
`/Users/zhuxiaowei/apps/rpa-mobile/CCOS/context/task-xpeng-gongzhonghao-phase1.md`。

该任务线当前聚焦公众号最新执行基线：以手工 `project/gongzhonghao/main.py` 为移动侧能力基线，补齐手机端与 PC 端文章采集机器人协作规则，并保持与视频号及主线框架解耦。

## 当前进展（同步摘要）

1. Node 侧最新权威规则已回写：当前移动侧能力基线以手工 `project/gongzhonghao/main.py` 为准，不再以“指定链接一期草案”作为唯一权威。
2. 公众号任务的调度入参骨架与回调结果骨架继续复用视频号现有统一结构，通过 `platform=11` 区分任务域。
3. 文章链接字段口径已冻结为 `Article.sign`；`articleLink` 只作为输入兼容名。
4. `targetContent` 已明确为历史遗留字段，视频号与公众号新契约中都不再使用。
5. 公众号 `actType=11` 继续保留 `likePost/lovePost` 作为有效预留参数；公众号域没有养号任务。
6. 新增双机器人协作规则：
   - 非文章内容采集任务：手机端直接回调，不调用 `device/release`。
   - 文章内容采集任务：手机端不直接回调，而是把完整回调出参作为 JSON 字符串转发给 PC 端文章采集机器人；PC 端机器人承担最终唯一回调；手机端仅在转发成功后调用 `device/release`。
7. 已新增独立手动测试脚本 `project/tests/test_xiaopeng_callback_release_manual.py`，用于人工验证小鹏 callback / device release 接口。
8. 已冻结 `actType=21/41` 的统一数量参数规则：`nums` 为视频号与公众号共用的外部标准必传字段；缺失时在参数适配层直接按参数异常失败，不再回退默认值。
9. 已冻结公众号文章内容补齐判定：`actType=32` 固定转发 PC；`actType=21/41` 仅当 `nums>0` 时进入“转发 PC + 释放设备”，`nums=0` 不转发。
10. 已新增公众号 `21` 的“多文章链接”联调用例草案：`GZH21-MULTI-SIGNS-SMOKE`，仅保留泛化的多 `articleData.sign` 审查口径，不再绑定特定真实文章链接样本。
11. 已将公众号评论菜单 OCR 独立探针从 `xp-wx1` 正式项目目录移出，收口到顶层 `cases/human_test/gongzhonghao_comment_menu_ocr_probe.py`，避免正式业务代码继续承载人工排障脚本。
12. 已冻结公众号兼容层 `commentData.articleId` 组装规则：只能回填同轮 `articleData.id` 的真实值；若移动侧当前结果没有真实文章 `id`，则必须置空，禁止再用文章链接 `sign` 顶替。
13. 已冻结小鹏主线统一收尾规则：外部调度任务在流程执行结束后默认补一次“回 Android 桌面”；内部 `task_type` 调试任务默认不触发，自定义 `cleanup_fn` 优先，回桌面失败只记日志不改任务结果。
14. 已冻结公众号对外结果出口规范化：`21` 仅回 `articleData + commentData`，`31` 仅回 `commentData`，`32` 仅回 `articleData`，`41` 回 `accountData + articleData + commentData`；同一份规范化结果同时作为小鹏 callback 与 PC 转发 `task_payload` 的唯一来源。
15. 已冻结公众号 `resultRowCount` 口径：按出口规范化后的主数据集重算，不再沿用执行框架内部默认 `1` 条包装计数；至少 `31=commentData 条数`、`32=articleData 条数`、`21=articleData 条数`、`41=accountData 条数`。
16. 已冻结公众号 `actType=41.pubTimeGe` 兼容规则：外部可传日期或带时分秒的日期时间字符串，但兼容层进入手写 `main.py` 前统一归一为 `YYYY-MM-DD`，避免手写脚本的日期比较逻辑报格式异常。
17. 已冻结公众号结果兼容层字段清洗规则：手写 `main.py` 中的 `title/content/images/videos/信息更新时间` 等占位值不得透传到对外结果；`articleData.pubTime`、`commentData.pubTime/updateTime` 对外统一收敛为毫秒时间戳，旧中文/字符串时间仅保留为规范化后的 `pubTimeStr`。
18. 已冻结公众号 `actType=41` 手写基线时间匹配口径：页面发布时间过滤统一兼容标准日期、中文绝对时间与相对时间文案，不再假设页面时间只会是单一 `YYYY年M月D日 HH:MM` 字符串。
18. 已冻结 `human_test` 与正式流程代码的依赖方向：统一 `examples/mobile-rpa-cases/cases/human_test/` 仅供人工直跑/排障探针使用；`project/**` 与导出流程包不得反向 import `human_test/**`，否则视为导包前预检错误。

## 下一步

1. 补跑公众号 `21/41/32` 真机链路，确认 `nums` 必传后的参数失败、`nums=0` 边界和 `nums>0` 转发链路都符合预期。
2. 用真实 attemptsId / callback 参数补跑公众号 `31` 真机链路，确认出口规范化后不再触发小鹏 callback `400`。
3. 继续清理历史文档中任何把 `21/41` 缺失 `nums` 视作默认 `10` 的旧描述。
4. 视需要把公众号后续可复用的时间解析 helper 沉淀到正式 `project/**`，不要再放在 `cases/human_test/**` 里被正式代码反向依赖。

## 待办任务（待审核）

1. `GZH-TODO-LEGACY-CLEANUP-01`：清理会误导后续 AI / 人工开发的公众号历史口径残留。
   - 高优先级候选：
     - `CCOS/assets/diagrams/xpeng-gongzhonghao-ai-auto-standard-chain-flow-v1.md` 仍保留“`21` 先手机端 direct callback”与“`32` 或显式 `collectArticleContent` 才进入补齐协作链路”的旧图示。
     - `CCOS/knowledge/business-logic/xpeng-project/dispatch-integration/05-回调结果契约与错误码基线-v1.md` 仍保留 `70011 = platform=11 当前未启用` 的旧口径。
     - `CCOS/knowledge/business-logic/xpeng-project/review/xpeng-phase1-testability-and-requirements-review-v1.md` 仍以“指定链接一期 / 只采链接 / `41`=公众号指定链接 / `21/32` 当前未支持 / 对客接口文档未冻结”为主描述。
     - `CCOS/knowledge/business-logic/xpeng-project/review/xpeng-task-param-adapter-review.md` 仍保留“`21/32/41` 可先不支持”“`lovePost` 仍依赖待拾取点位”等旧评审结论。
   - 中优先级候选：
     - `examples/mobile-rpa-cases/cases/xp-wx1-simplified/project/param_adapter.py` fallback 文案仍包含“公众号指定链接流程尚未接回，当前仅支持 actType=21 mock 快捷链路”。
     - `examples/mobile-rpa-cases/cases/xp-wx1-simplified/project/gongzhonghao/contracts.py` 仍保留未被主流程使用的 `build_specified_link_task_payload` 旧命名兼容入口。
   - 低优先级候选：
     - 任务线与上下文文档中的“文章内容采集”旧措辞，统一替换为“文章内容补齐”。
   - 执行前提：
     - 先由用户审核确认哪些项判定为“已过时且应移除/降级为历史”，再执行最小改动清理。

## Progress Log

- 2026-04-09 08:55 +0800 已按用户确认收口公众号“指定链接文章页判定”观察策略：继续沿用手写 `project/gongzhonghao/main.py` 里的旧单一信号，不引入视频号那套多信号页面刷新判定；当前只把 `profileBt` 单信号等待从 `50000ms` 提升到 `90000ms` 继续观察偶发概率，避免在公众号人写基线上做额外结构性改动。

- 2026-03-04 17:33:45 +0800 任务线启动：完成 Node/Hub 路由同步与行动计划审阅稿落盘，待用户审核后执行。
- 2026-03-04 17:38:00 +0800 用户新增“app.py 冻结 + 独立目录模块化实现”原则，已同步 Node/Hub 任务线文档。
- 2026-03-04 20:15:00 +0800 并发路由迁移：Node 侧改为 `task-index + agent-focus`；`STEP-4` 代码与测试产出已完成。
- 2026-03-04 21:40:00 +0800 `STEP-5` 代码与单测完成；守卫命令通过，进入 `GZH-06` 真机验证阶段。
- 2026-03-05 11:20:00 +0800 公众号代码迁移到 `xp-wx1` 正式版目录并同步案例索引/文档，POC 目录重命名口径完成。
- 2026-03-20 11:30:00 +0800 用户确认最新协作口径：文章内容采集场景由手机端转发到 PC 端文章采集机器人并由 PC 端承担最终单次回调；手机端仅在转发成功后释放设备。
- 2026-03-20 11:45:00 +0800 已同步 `Article.sign`、`targetContent` 废弃、公众号无养号、`actType=11` 保留 `likePost/lovePost` 到 Node/Hub 任务线与 PRD。
- 2026-03-21 16:35:00 +0800 已收口 `nums` 必传规则：`actType=21/41` 缺失 `nums` 在参数适配层直接失败；公众号 `32` 固定转发 PC，`21/41` 仅 `nums>0` 转发，相关 PRD/测试链路文档/适配层单测已同步。
- 2026-03-21 16:55:00 +0800 已把“公众号历史口径清理候选清单”登记为任务线待办：优先覆盖旧链路图、错误码基线、两份 review 文档、fallback 旧报错文案与 `specified_link` 兼容入口，待用户审核后按最小改动执行。
- 2026-03-21 17:05:00 +0800 已在公众号 `21` 联调子文档新增 `GZH21-MULTI-SIGNS-SMOKE` 草案：以 `nums=3` 验证多条 `articleData.sign` 返回。
- 2026-03-21 17:20:00 +0800 已移除误登记的真实文章链接锚点：该链接与公众号 `21` 任务无关，文档与任务线仅保留泛化多文章链接审查口径。
- 2026-03-21 19:20:00 +0800 已把公众号评论菜单 OCR 探针迁出 `xp-wx1-simplified/project/**`，改为顶层 `cases/human_test/gongzhonghao_comment_menu_ocr_probe.py` 自包含脚本；保留流程内截图 OCR 与截图后稳定等待，避免正式项目继续承载独立人工排障入口。
- 2026-03-24 10:35:00 +0800 已补充公众号兼容层冻结规则：`commentData.articleId` 不得再回填文章链接 `sign`；兼容层仅允许绑定真实 `articleData.id`，拿不到时返回空串，并已补对应回归测试。
- 2026-03-24 11:40:00 +0800 已补充小鹏主线统一收尾规则：外部调度任务结束后默认执行一次回 Android 桌面；内部 `task_type` 调试任务默认不触发，自定义 `cleanup_fn` 优先，回桌面失败仅记日志不改结果。对应代码收口在 `app.py`，入口回归测试已补齐。
- 2026-03-25 00:15:00 +0800 已收口公众号回调出口规范化：`reporting.py` 新增公众号 act 结果裁剪层，统一按 `21/31/32/41` 对外字段冻结规则删除不该回调的数据块，并把 `resultRowCount` 改为按裁剪后的主数据集重算；本地已通过 `test_reporting_signature.py`、`test_gongzhonghao_flow.py`、`test_entry.py` 与 `check_rpa_device_test_convention.py` 回归，待用真实 `31` 参数复测小鹏 callback `400` 是否关闭。
- 2026-03-25 00:35:00 +0800 已补公众号 `41.pubTimeGe` 兼容修复：兼容层现在接受 `2026-03-22 13:00:00` 一类带时分秒的输入，并在进入手写 `main.py` 前归一为 `2026-03-22`；已补 `test_gongzhonghao_flow.py` 回归，守卫检查通过。
- 2026-03-25 16:20:00 +0800 已补公众号兼容层旧结果清洗：`executor.py` 现在会在组装 `articleData/commentData/accountData` 时统一剔除手写 `main.py` 的 `title/content/images/信息更新时间` 占位值，并把文章/评论时间字段统一转成毫秒时间戳；已补 `test_gongzhonghao_flow.py` 回归并复跑 `test_reporting_signature.py`、`test_videohao_collect_runtime.py` 与 `check_rpa_device_test_convention.py`，确认未影响视频号链路。
- 2026-03-25 17:18:00 +0800 已修复手写 `main.py` 的 `actType=41` 时间链路两处问题：`time_change()` 不再错误调用 `datetime.datetime`；`is_first_later()` 与页面时间 helper 已统一兼容 `YYYY-MM-DD`、中文绝对时间与相对时间文案，避免账号采集在发布时间过滤阶段再次因格式不兼容崩溃。本地已补 `test_gongzhonghao_flow.py` 回归并复跑守卫检查，下一步在 `s2` 真机补跑一轮 `41`。
- 2026-03-25 17:20:00 +0800 已排查“导出流程包前预检无法解析 `human_test.page_time_helper`”问题：根因不是视频号稳定基线变更，而是本地误把给公众号/人工测试抽取的时间 helper 反向接入了视频号 `project/**`，导致导包运行时 `sys.path` 无法解析统一 `cases/human_test/`。当前已恢复视频号运行时代码到稳定基线，并在 `check_rpa_device_test_convention.py` 中新增 guard，禁止任何 `project/**` 反向 import `human_test/**`；同步更新案例目录规范与导包规则文档，明确 `cases/human_test/` 仅供人工直跑/探针使用。
