-
- 已完成/已验证通过
	- UAT联调问题排查：账号采集回来的文章之后再定向帖子采集评论找不到文章，已确定为微信检索自身问题，提供降级刷视频检索解决方案，沟通确认，功能开发自测及问题修复
	- 创作者视频主页中可能因网络问题出现“轻触空白处，再次加载。”问题：增加点击再次加载逻辑，开发自测及问题修复
	- 采集时间判断问题：时间匹配逻辑回归测试
	- 采集文章信息得到的时间不对：时间字符串调整为展示原页面中显示的时间信息
	- 采集文章计算时间存在偏差问题修复
	- 自测问题修复：采集数量入参逻辑问题修改，统一调整为每条视频的评论采集数量，移除原总采集数量逻辑
	- 运行记录在流程开始时会进入丢失结果问题排查解决
	- 增加回调接口调用开关，本地测试时只使用本地mock服务，不再走回调服务接口，开发自测及问题修复
	- 小鹏调度系统对接问题沟通、解决方案确认：任务回调超时问题解决方案，目前存手机占用状态对调度系统不透明问题，考虑增加对接接口查询任务执行状态
	- 自测问题修复：userCode在网络环境差时还是存在采集不到返回未知的情况，已调整等待时长和元素查找时长，增加稳定性
	- 回调超时问题解决方案
- 明日

- 日报
	- 今日完成
		- 提高弱网场景下 `userCode` 采集稳定性，延长“更多信息”入口与锚点文本读取等待，降低回落到“未知”的概率。
		- 将 `21/31/41` 的 `commentNums/comment_count` 收口为“每个命中视频单独生效”，同步更新 collect/combo 测试与联调样例。
		- 恢复 `pubTimeStr` 页面原文透传，并让 `targetPubTime/pubTimeGe` 兼容 `10/13` 位时间戳输入。
		- 收口 `callbackUrl` 规则：本地 `127.0.0.1/localhost` 只回 mock，未传继续走正式 callback，显式外部地址按参数错误处理。
		- 新增门户任务拉取/上报链路文档和时序图，回填 `s2` 误报告警根因与轮询策略更新，便于后续稳定性排查复用。
	- 明日任务
		- 在指定设备补跑弱网 smoke，验证 `userCode` 与“每视频评论额度”在真实链路中的表现。
		- 继续校验 callback-only 本地 mock 行为、时间字段透传与回调结果口径。
		- 继续沿稳定基线和门户链路推进真机 smoke 与文档同步。
	- 工时
		- 4.6h（按 `rpa-mobile` 2026-03-19 git 提交元数据累计）
-
