- xp wx项目需求文档更新同步成最新实现版本
	- 基于当前实现编写入参、出参格式说明，重构流程图
	- docx格式排版优化
-
- 新版流程包开发
	- 增加网络相关日志补充
	- 信息采集脚本更新完善
-
- 日报
	- 今日完成
		- 完成 `wx项目 (2)` 第二章需求文档同步：基于当前实现补齐入参、出参说明，重构 Mermaid 流程图，并把原始 `.docx` 收口为 Markdown / Mermaid 可维护基线。
		- 基于 `workUuid/workId` 失败样本补齐 AI 自动排障链路：在 `rpa-dev-platform` 落地 `get_cloud_execution_logs / analyze_cloud_execution / troubleshoot_cloud_execution`，支持按 portal engine log 自动分析失败原因、透出 `config.source/config.endpoint`，并把口语 `workId` 统一先按 `workUuid` 候选处理。
		- 收口小鹏视频号标题匹配 bug：针对云端失败样本完成“`workUuid` -> 云端日志 -> `s2(WKDAUGWGEA75KRCM)` 真机复放 -> 修复 -> 二次回归”闭环，修复双侧截断标题仍被误判为未命中的问题，并冻结“优先以播放页真实标题和迭代日志判责”的口径。
		- 修复小鹏 `videohao after_all_acts` 风控误报：补齐云端原始 `task_payload` 提取与 `observed_task_payloads` 真机回放步骤，修复已经回到 `VIDEO_PLAY` 后仍抛 `RuntimeError` 的问题，并补齐 `risk_control` 回归测试。
		- 同步小鹏稳定基线业务真值：把标题匹配、云端失败样本复放、mock 回归策略和 `host_preflight` 代理误报判责规则沉淀进稳定基线与协作文档，避免后续 AI 排障继续手写近似参数或把工具配置问题误判成业务失败。
		- 同步塔斯诊断日志与 commander 稳定性两条任务线：补录客户诊断采集脚本交付说明、网络与平台状态证据面、离线分析定责摘要，以及 commander 进程重启恢复、async report、durable replay 的真机验证结论。
		- 推进新版流程包开发，补充网络相关日志和信息采集脚本细节，并把诊断采集脚本的 `--issue-desc` 默认时间戳、`--help` 与最简使用说明入口一并补齐。
		- 梳理与 Codex 协同的 RPA 操作方案：尝试通过 MCP 点击进入微信，再让 Codex 读截图参与判断，并识别出“流程执行时序与截图观察时间可能错位”的核心风险。
	- 明日任务
		- 继续沿塔斯诊断与 commander 任务线补成功确认信号与真实失败 bundle 验证，收口更难归因的结果丢失场景。
		- 持续收集 `s2` 上剩余的小鹏稳定基线回归证据，优先沿用云端原始 `task_payload` 做真机复放，不再手写近似参数，并继续复用新的 cloud troubleshoot workflow。
		- 继续收口 `wx项目 (2)` 第二章文档与流程图资产，保持实现、对客文档与本地阅读稿三者一致。
		- 持续完善新版流程包与 Codex 协同链路，重点解决截图观察与设备实时状态不同步的问题。
	- 工时
		- 至少 20.2h（按 `apps/rpa-mobile` 中已确认的 `wx项目 (2)`、小鹏稳定基线、塔斯诊断与 commander 相关 commit 元数据累计；未把 `ccos` 侧任务线同步与未提交探索重复计入）
-