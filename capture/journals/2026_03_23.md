- 已完成
	- 回归测试验证回调释放设备接口是否生效
	- 投诉、不喜欢 功能对接
	- 公众号流程测试
	- 小鹏调度系统任务超时-运营平台队列超时记录物理删除问题排查：确定为设备环境中的耗电量策略导致app被系统关闭，进而导致设备上的塔斯app被关闭接不到任务，进而导致运营平台任务队列超时记录被删除，进而导致调度系统任务超时
	-
- 小鹏项目基础设施
	- 目前缺少远程任务排错功能，可以通过botId远程下发任务获取系统日志信息进行AI分析的逻辑
- 日报
	- 今日完成
		- 完成视频号非养号任务的 task 级通用风控升级：新增顶层 `riskLevel=1/2/3`，在播放页执行前后置风控，当前动作保留“刷视频 / 点赞视频 / 喜欢视频”，并明确 `actType=51` 忽略该字段；相关执行编排与风控 runner 单测已补齐。
		- 完成稳定基线文档同步：补记视频号通用风控规则、`51` 忽略口径、后续 `s2` 真机回归范围，并统一手动停止对客码文档口径，明确 `50012` 仅保留为历史兼容 stop 语义。
		- 完成视频号定向链路与 act31 稳定性修复：恢复 single 定向链路成功截图与 `act_observer` 透传、复用搜索命中缓存避免重复进评论页，并加固 `act31` 评论入口空指针风险与 activity 诊断，`s2` 真机 mock 回归已通过。
		- 完成公众号主流程与兼容层同步：提交手动主流程与转发器调整，修正 executor 对 `pubTimeStr/pubTime/updateTime/likes/replys` 的槽位映射，补齐新旧格式兼容测试，避免 `21/31/41` 的 `commentData` 错位。
		- 完成公众号“投诉 / 不喜欢”功能对接，并继续推进公众号流程测试与 callback 释放设备接口回归验证。
		- 排查并定位“小鹏调度系统任务超时”问题，确认根因是设备耗电策略导致 App 被系统关闭，连锁引发塔斯 App 接不到任务、运营平台队列超时记录被删除，最终表现为调度系统任务超时。
		- 梳理出小鹏项目当前基础设施缺口：缺少基于 `botId` 的远程任务排错能力，尚不能远程下发诊断任务并拉取系统日志给 AI 分析。
	- 明日任务
		- 在 `s2` 上补做 `21/41/3x/1x/51` 与公众号 smoke 的专项回归，确认前后风控时机、真实页面动作、总停留时长与最终 callback 顺序。
		- 继续补跑公众号 `21/31/41` 真机链路，确认新的评论时间字段、点赞数和回复数在 callback 中都符合预期。
		- 基于排查结论，继续收口设备耗电策略导致的任务超时问题，补环境治理与验证口径。
		- 设计并推进基于 `botId` 的远程任务排错能力，打通“远程下发任务 -> 获取系统日志 -> AI 分析”链路。
	- 工时
		- 7.1h（按 `rpa-mobile` 2026-03-23 git 提交元数据累计）