- 小鹏项目联调问题记录
	- 待修复
		- 微信导航器的覆盖逻辑有问题，要让AI明确，优先以用户流程目录中已有导航器为准，不允许用开发平台的导航器去覆盖流程代码，只有用户明确说明时才可以使用，这个导航器到底如何解决同步问题，考虑要出一个版本号信息，每次文件改动时，AI都需要同步升级版本号，当用户版本与开发平台版本不一致时，要考虑合并升级开发平台版本
		-
		-
		-
-
	- 已修复待验证
		- 41
		- 51
			- 调用模型生成评论的功能中要增加稳定性优化，当模型服务接口重试超过5次失败时，或者返回的内容为空、有问题时，将当前act标记为失败，继续下一个任务，不要影响整体流程
		- 11
			- 11任务支持喜欢视频、点赞视频操作
		- 3x
			- 3x任务的在创作者页面进入搜索页面搜索视频+点击视频逻辑还没有实现，目前是不通过的状态
			  collapsed:: true
				- 进入创作者页面后，点击zbot.selector().desc("搜索").clickable().visibleToUser()元素，进入到搜索内页，
				- 在输入框元素zbot.selector().className("android.widget.EditText").clickable().visibleToUser()中输入要搜索的目标视频标题，
				- 点击zbot.selector().text("搜索").className("android.widget.TextView").clickable().visibleToUser()元素进行搜索，
				- 等待页面加载（先默认等待3s），
				- 通过zbot.selector().text("搜索更多相关内容").className("android.widget.TextView").visibleToUser()相对定位搜索到的第一个视频，点击即可进入视频播放页面，继续执行后续任务
				- 参考代码：
				- more_ele=zbot.selector().text("搜索更多相关内容").className("android.widget.TextView").visibleToUser().findOne(20000)
				- zbot.automator.clickCenter(more_ele.parent().parent().child(0).child(0).child(0))
			- 31任务，我只下发了一个采集评论的act，已经匹配到目标视频并完成评论采集后，还是会继续跑刷视频的逻辑，需要实现act执行完成退出的标准逻辑：同一视频下的评论相关act执行完后就应该退出当前视频，同一创作者下的所有act执行完后就应该退出创作者页面，task下所有act结束就应该立即结束任务，这个不光31任务，其他所有涉及到的都需要看一下统一改掉
			- 所有的非采集任务act在执行完核心动作后（比如点赞对应执行完点击点赞元素动作、评论在执行完发送操作瞬间）就应该截图，这个需要出设计方案判断怎么改，因为之前考虑在通用框架中统一实现，这样解藕性会更好，需要看一下是否有耦合性低的方案还是必须写在流程代码中
			- 目前应该只有两种需要截图的情况，流程异常停止、失败停止等直接截图，这部分应该写在通用框架中？另一种是关键场景截图，比如非采集任务每个act核心动作完成，以及养号任务每个核心动作完成也需要截图
			- 所有act关键节点成功截图应该截取执行成功瞬间的图，目前是不满足的，比如13点赞成功会退出评论页面才截图
			- 在创作者页面搜索视频进入的逻辑中，只有目标创作者下只有一个目标视频的定向任务时才使用在创作者页面定向搜索的逻辑，如果一个创作者有多个目标视频还应该使用之前的视频迭代器匹配的方式去找，应该涉及到3x和1x的任务
-
	- 已验证通过
		- 41任务采集到的发布时间戳还是有问题，转化之后显示的是1970-01-21 xxxxx，看下所有的这个时间相关的逻辑，都需要改正确，不要再出现这种错误
		- userCode问题：_collect_account_user_code_from_more_info_dialog方法在入参传递了userCode后会跳过采集code逻辑，返回入参的userCode
		- 所有截屏照片存放位置改为在taskResult中以“,”拼接返回
		- 进入创作者视频主页后进入首个视频页面方法需要调整，目前存在不支持的创作者页面结构
		-
- app优化
	- 待实现
		- 流程停止还是有问题，目前流程在频繁检索元素的过程中，或者find命令执行时间过长时，停止任务无法中断
		- 元素唯一性问题，增加点击方法，如果目标selector不唯一直接报错
		- click点击方法，如果目标clickable=False直接报错
		- agentService任务拉取中，跳过本次轮询请求，这种情况出现时，线程轮询速度非常快，需要考虑优化一下
	- 已实现
		- 增加了接收到下发任务时自动亮屏+解锁屏幕执行任务功能

- 日报
	- 今日完成
		- 完成 `xp-wx1-simplified` 首轮结构收口：拍薄 app 入口、抽公共 dispatch 规则、继续拆小 helper，降低流程入口与分发耦合。
		- 完成视频号创作者主页首个视频导航修复，移除 `is_external` 主链分叉，并冻结“稳定事实代码优先”的选择器/导航规则。
		- 完成 creator 级 combo 的“采完即停”和关键成功截图收口，补齐专项回归文档与后续 `s2` 验证准备。
		- 完成作者页视频路由与 probe 规则回调，统一 `1x/3x` 命中口径、`targetTile` 术语以及 `1x-011` 文档/测试样例。
		- 完成 `rpa-dev-platform` 设备重连生命周期与插件恢复链路加固，并扩展设备预热前置条件。
		- 完成 Android 亮屏/解锁与安全锁屏 blocker 版本升级，同步非安全锁屏真机回归结果。
		- 完成塔斯无障碍稳定性 taskline/知识索引初始化，为 `ACC-01/ACC-02` 建立外层路由。
	- 明日任务
		- 补跑 `s2` 上导航器、creator 级退出和成功截图相关回归，确认真实页面时机与退出链路。
		- 继续推进 `xp-wx1-simplified` 结构优化、`31/32 targetTile` 适配和剩余 runtime cleanup。
		- 继续验证 AI 评论失败按 `act` 降级、设备预热 readiness gate 和安全锁屏 blocker 的真机闭环。
	- 工时
		- 24.2h（按 `rpa-mobile` 2026-03-12 git 提交元数据累计）
