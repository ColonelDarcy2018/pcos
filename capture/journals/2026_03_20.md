- 沉淀塔斯app待优化点：
	- 报错信息看不到报错行数和堆栈信息，不方便开发者排查问题
	- 大型脚本项目协作开发模式：版本控制，协作编辑，模块引用
	-
- 核心任务
	- 视频号稳定性问题自测验证及修复
	- UAT联调测试配合、问题支撑及问题修复
	- 稳定性问题修复
	- 风控功能调整完善
		- 登出状态检测机制，需要在流程执行开始时增加
		- 中途账号登出，被挤掉，需要还原场景测试
	- 多act流程规划功能稳定性测试及问题修复
-
- 低优先级
	- 置顶视频跳过逻辑完善：采集置顶元素判断置顶视频总数量，目前设置的最大值是5，未跳过
	- 导航超过步数导致流程执行失败问题：考虑增加导航过程中检测目标app是否开启状态逻辑，如果中途异常切换到其他app，或者异常退出了应用，需要重新开启app
	- 需要增加检测微信登陆页面逻辑，如果处于登陆页面，提示用户请先登陆app
	- 稳定性问题：关键词检索结果页面概率性出现因网络问题导致刷新不出视频列表问题，且刷新不出视频的情况下依然继续执行了视频迭代器的逻辑，需要确定进入视频迭代器是否有检测页面的保险逻辑
	- 公众号评论输入框元素手点击进入可以拾取，rpa点击进入不可拾取
-
- 待修复
	-
	- 创作者视频列表页面执行报错：
		- | 2026-03-20 16:38:13 | [PLAN_EXCEPTION] AttributeError: 'NoneType' object has no attribute 'click' | 其他 |
		  | 2026-03-20 16:38:13 | [PLAN_EXCEPTION] zbot.selector().indexInParent(3).className(CLASS_LINEARLAYOUT).clickable().visibleToUser().findOne( | 其他 |
		  | 2026-03-20 16:38:13 | [PLAN_EXCEPTION] File "/storage/emulated/0/Android/data/com.aiindeed.mobileagent/files/rpa/runtime/20260320_163611_569/project/videohao/collect_runtime.py", line 166, in jump_to_comment_page |
	-
	-
- 待回归验证
-
- 已完成/验证通过
	- 回调超时解决方案：
		- 方案1-小鹏调度系统对接获取信息接口
			- 1. 每次下发前通过workid调用运营平台接口查询运行状态，如果是运行中说明设备还在运行
			- 2. 如果需要强制下发，可以先调用停止流程接口，等待流程停止后，再调用下发接口
		- 方案2-使用协议超时时间
			- 在小鹏调度系统能确保同一个设备只有一个任务+收到回调或者回调超时前不允许再下发任务的前提下，小鹏调度系统40分钟超时，流程内30分钟超时，提供10分钟的缓冲容错空间；
	-
	- 部分文章标题在创作者页面中进行搜索存在找不到的情况，改用视频迭代器匹配策略，当组合任务时，默认直接使用迭代器检索，如果是单一act，使用搜索的方式检索，检索不到目标视频时再降级策略使用视频迭代器去匹配，视频迭代器匹配的过程中还要注意时间的匹配机制：第一是在匹配目标视频时要保证时间和标题都一致才算匹配成功，第二需要通过时间判定来停止视频迭代逻辑，这个可以参考ge时间的逻辑
	- 稳定性自测问题修复：3x组合任务视频检索逻辑优化；定向任务入参校验优化；组合任务代码优化；
	- 测试用例评审
	- 小鹏UAT测试问题：时间需要调整成精确到日期的形式，避免精确到秒造成的小鹏方文章重复判定问题
	- 公众号开发技术支持
	- 联调调度系统测试公众号流程释放手机设备接口
	- 小鹏调度系统接口、流程更新接口沟通对接使用方式+自测使用
	- 视频号流程UAT测试问题答疑、相关问题排查：调度系统下发任务超时未回调问题排查
	- 运营平台运行记录丢失问题排查、与运营平台开发沟通确认具体原因
	- 公众号参数对接标准入参结构、完整测试链路mock方法实现，联调测试及问题修复
	- 标题定向检索逻辑优化，使用时间作为截止条件判定，增加流程稳定性

- 日报
	- 今日完成
		- 修正 `1x/3x` planning 语义：同一创作者分组内单 `act` 保持 `single plan`，多 `act` 才走 `combo plan`，并新增统一 `family plan helper`。
		- 恢复定向视频任务命中链路：单 `act` 作者页搜索优先、失败后回退视频迭代器；组合任务默认直接走迭代器，要求标题与发布时间同日同时命中。
		- 新增公众号域独立 `main.py` 入口，先把导航、搜索、文章链接采集与辅助方法沉淀为单文件主流程。
		- 收口塔斯助手/悬浮按钮开关失败提示和原生返回口径，避免无障碍总开关关闭时前端误判为已开启。
		- 同步稳定基线 taskline，明确 executor 内 `single/combo` 收口重构仅冻结为 `SB-19 backlog`，避免误判已落地。
	- 明日任务
		- 补跑 `31/11` 单 `act` 与 combo 真机 smoke，确认 planning 语义稳定。
		- 继续拆分公众号主流程剩余目录，补齐 planning/test 配套与后续真机验证。
		- 在空闲设备验证悬浮开关失败提示、预热链路和浮窗就绪状态。
	- 工时
		- 2.7h（按 `rpa-mobile` 2026-03-20 git 提交元数据累计）
-
