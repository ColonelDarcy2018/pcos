- 日报
	- 今日完成
		- 完成公众号联调测试技术支持，并排查修正 PC 机器人采集信息回调 `400` 问题：明确文章采集 `pubTime` 需改为毫秒级、评论 `articleId` 必须为 `uuid`、`images` 需改为字符串拼接。
		- 完成公众号兼容层规则冻结：`commentData.articleId` 禁止再回填文章链接 `sign`，只能绑定真实 `articleData.id`，缺失时返回空串，并已补对应回归测试。
		- 完成小鹏主线统一收尾能力补充：外部调度任务结束后默认执行一次“回 Android 桌面”，内部 `task_type` 调试任务默认不触发，自定义 `cleanup_fn` 优先，回桌面失败只记日志不改结果；入口回归测试已补齐。
		- 完成公众号 `act14/15` 路由修复，并回收最新手动 `main` 快照作为当前人工基线，便于后续继续做外层兼容集成。
		- 同步 Android runtime 优化进展：Android 子仓已推进到包含门户 `takeWork` 幂等重试、`PORTAL_TAKE_TRACE` 与低内存诊断日志的版本，并把 `takeWork/report` 五态状态机、`STARTING` 窗口、首次同步上报和 `s4` 低内存诊断事实正式回写到任务线文档。
		- 手机设备运行环境稳定性问题排查分析：确定当前稳定性主要问题由网络导致任务丢失、微信视频号内存飙升导致系统清理应用导致
		- pc机器人采集信息调用回调接口400问题排查解决:文章采集信息pubTime需要改为毫秒级；评论articleId需要保证为uuid；images格式改为字符串拼接
		-
	- 明日任务
		- 公众号联调及相关接口返回问题排查修复
		- 稳定性问题监控修复
		- 继续确认运营平台显示的机器人在线状态是否准确，并据此收口设备在线判断逻辑。
		- 继续补跑公众号 `21/41/32` 端到端链路与 `114` 真机 smoke
		- 真机验证 `PORTAL_TAKE_TRACE` 与 `MEM_DIAG` 证据链，并继续推进基于 `botId` 的远程任务排错能力。
		-
		- 继续排查公众号评论输入框“手点可拾取、RPA 点击后不可拾取”的差异原因。
		- 真机验证 `PORTAL_TAKE_TRACE` 与 `MEM_DIAG` 证据链，并继续推进基于 `botId` 的远程任务排错能力。
	- 工时
		- 2.4h（按 `rpa-mobile` 2026-03-24 git 提交元数据折算；其中 4 条提交的 `hours` 未带 `h` 后缀，已按小时口径计入）