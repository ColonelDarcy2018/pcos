- 待修复
	- 点赞操作要能判断是否已经点过
	- 养号任务支持拟人级别入参：高、中、低
- 已修复待验证
	- "userCode": "未知"问题
		- 传空字符串、不传code时应执行code查找逻辑，返回正确code
	- 采集数据量不足问题，commentNums是要采集到的评论数量，不是评论迭代器的循环次数

- 日报
	- 今日完成
		- 完成视频号采集与 `1x/3x combo` 相对发布时间边界修复，把结果页等待调整为更保守的长等待 + 锚点复核方案。
		- 继续拍平 `xp-wx1-simplified execution`：收口 `actResult` 更新、只接受 `task_payload` 本体，并继续推进 validation funnel 精简。
		- 修复 `AgentSocketService` 将主动断开误判为网络异常导致的重连风暴，补齐链路图与 `s2` 真机排查结论沉淀。
		- 清理 `human_test`、`_unmaintained` 与旧 mock 输出目录回流问题，增加已有导航器与 `platform_sdk` 文件不自动覆盖保护。
		- 完成 `AISTD R06/R07/R08` 真机记录、真实样本时间对齐及 `51/41/21/3x/1x` 回归文档同步。
		- 修复 `userCode` 占位值与 `commentNums` 数量口径问题，补齐 `21/31/41/3x` 相关保护测试。
	- 明日任务
		- 继续补真实设备回归，验证 collect timing、`commentNums` 和 `userCode` 的真实链路表现。
		- 继续精简 `execution.py` 与 `simplified flow`，保持结构收口但不重新分层。
		- 继续收口 callback `failMsg` 细节、`51` 成功后未退出问题和历史文档清理。
	- 工时
		- 9.5h（按 `rpa-mobile` 2026-03-13 git 提交元数据累计）
