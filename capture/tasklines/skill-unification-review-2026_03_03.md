# 技能统一审核稿（2026-03-03）

## 目标

1. 移除项目内本地技能副本，统一使用全局技能（`~/.codex/skills`）。
2. 消除“同名技能多版本”歧义，降低 AI 入口判断和上下文负担。
3. 冲突处理遵循：保留信息更全面、更可用、更合理的规则。
4. `mobile-rpa-script-dev` 必须保留全局“元素拾取协议”段落。

## 现状盘点

- 全局技能路径：`/Users/zhuxiaowei/.codex/skills/*/SKILL.md`
- 本地副本路径：`/Users/zhuxiaowei/apps/rpa-mobile/.trae/skills/*/SKILL.md`
- 差异结论：7/7 个同名技能均存在内容差异。

## 统一策略（建议）

- 基线采用全局技能版本。
- 将本地版本中“增量且有价值”的规则回灌到全局技能。
- 本地版本中“删减导致能力退化”的内容不采纳。
- 全局更新完成并验收后，优先采用“软链接兼容模式”：
  - 本地 `.trae/skills` 不再存放独立内容，改为指向全局技能目录或同名技能软链接。
  - 这样既保持“单一事实源”，又兼容可能仍依赖本地路径的工具。

## 技能级对照（拟修改）

### 1) game-requirement-innovation

- 保留（全局）：现有流程与输出契约。
- 并入（本地增量，4条）：
  - knowledge 文档需面向非对话读者。
  - 同主题迭代维护版本变更记录表。
  - 过程信息写入 `CCOS/context/**`。
  - 最小必要修改，删除需记录原因。
- 舍弃：无。

### 2) ccos-protocol-bootstrap

- 保留（全局）：协议层级、P0 约束、初始化流程。
- 并入（本地增量，4条）：
  - knowledge 文档禁止过程化表述。
  - 单文档迭代 + 版本变更记录表。
  - 过程信息归档到 `context/**`。
  - 最小必要修改与删除说明。
- 舍弃：无。

### 3) mobile-rpa-script-dev

- 保留（全局，必须）：
  - “人机协作元素拾取协议”完整段落。
  - `PICK_TASKS.md` 要求与相关 reference。
- 并入（本地增量，2条）：
  - 修改脚本/配置遵循最小必要修改。
  - 删除既有逻辑必须说明删除项、原因与影响范围。
- 舍弃（本地删减，不采纳）：
  - 删除元素拾取协议的改动。
  - 删除对应 reference 的改动。

### 4) 上下文管理

- 保留（全局）：上下文恢复/保存主流程。
- 并入（本地增量，3条）：
  - context 与 knowledge 的写入边界。
  - 上下文转知识文档需去过程化表达。
  - 最小改动与删除说明约束。
- 舍弃：无。

### 5) 业务逻辑图谱

- 保留（全局）：图谱输出流程与协同机制。
- 并入（本地增量，3条）：
  - 面向非对话读者写作。
  - 版本变更记录表。
  - 最小改动与删除说明。
- 舍弃：无。

### 6) 代码审查

- 保留（全局）：审查流程、分级结论与输出契约。
- 并入（本地增量，4条）：
  - 评审结论沉淀到 knowledge 时去过程化。
  - 同主题单文档迭代 + 版本变更记录表。
  - 过程沟通写入 context。
  - 最小必要修改与删除说明。
- 舍弃：无。

### 7) 任务分解

- 保留（全局）：拆解流程、依赖风险、确认点。
- 并入（本地增量，4条）：
  - knowledge 文档面向非对话读者。
  - 单文档迭代 + 版本变更记录表。
  - 过程信息写入 context。
  - 最小必要修改与删除说明。
- 舍弃：无。

## 执行顺序（审核通过后）

1. 更新全局技能 `~/.codex/skills`（7个文件）。
2. 运行技能一致性检查（同名技能 hash 对比）。
3. 清理 `apps/rpa-mobile/.trae/skills/*` 本地副本内容，改为软链接到全局技能（或按你要求硬删除）。
4. 在 `apps/rpa-mobile/AGENTS.md` 增加一句“统一使用全局技能”。
5. 回归验证（抽样触发 2-3 个技能，确认解析入口一致）。

## 风险与回滚

- 风险：硬删除本地技能后，若某些工具仍硬依赖 `.trae/skills`，可能导致入口回退。
- 缓解：优先软链接模式，并保留 1 个发布窗口（例如 1-3 天）观察；必要时再切换为硬删除。
- 回滚：恢复 `apps/rpa-mobile/.trae/skills` 目录或取消软链接即可。
